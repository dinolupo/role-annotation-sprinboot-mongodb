spring:
  data:
    mongodb:
      database: UserRegistrationDB
      host: localhost
      port: 12345
  
temotec:
  annotations:
    security:
      collection: User
      role-path: roles
      username-path: username
  
